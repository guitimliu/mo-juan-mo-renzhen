# docker compose 用：cp .env.example .env 後改；.env 不會進 git。
# ======== 共用 ========
# stub＝離線示範（正本改寫，非 AI）；bedrock＝真的走 AWS Bedrock（下面 AWS 段必填）
ADAPTER=stub
# stub 模式每階段模擬延遲（秒）；bedrock 模式忽略
STUB_STAGE_DELAY_S=1.5
# nginx 對外埠（雲端部署建議 80）
FRONTEND_PORT=8080
# 後端直連埠（只給 curl／除錯；前端走 nginx 同源 /api，不需要開對外）
BACKEND_PORT=8000
# ======== 登入（選填）========
# 兩個都設才會要求登入（前端出現登入頁；/api/cases* 需 Bearer token）；留空＝不驗證，本機開發／stub demo 直接用
# AUTH_USERNAME=clerk
# AUTH_PASSWORD=change-me
# token 簽章金鑰；不設則每次啟動隨機（重啟後要重新登入）。多個 backend 副本時必設同一值
# AUTH_SECRET=
# token 有效秒數（預設 12 小時）
# AUTH_TOKEN_TTL_S=43200
# 只有前端跨網域直打後端時才要改；經 nginx 同源代理用不到
# CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# ======== Bedrock 模式（ADAPTER=bedrock 才需要）========
# 方式 A：EC2 掛 IAM instance profile（建議）→ 三個 AWS_* 留空，boto3 自動取用。
# 方式 B：主辦方臨時憑證（ASIA…，數小時過期）→ 填三個 AWS_*；過期後重填並 docker compose up -d --force-recreate backend
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# 也可用 AWS_DEFAULT_REGION；KB 與 inference profile 都在 us-west-2
# BEDROCK_REGION=us-west-2
# Knowledge Base（見 backend/README.md「Knowledge Base」）
# BEDROCK_KB_ID=ZOMMOWFOT2
# ---- 模型（全部可設定；預設四階段都用 Claude Sonnet 5；Claude 要用 us. 開頭的 inference profile）----
# 四階段共同預設
# BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-5
# 帳戶拿不到預設模型時自動降級（設空字串關掉）；/api/health 會標示
# BEDROCK_FALLBACK_MODEL_ID=us.anthropic.claude-sonnet-4-6
# 以下四個留空＝用 BEDROCK_MODEL_ID；可個別覆寫，例如 retrieval 換便宜的 us.anthropic.claude-haiku-4-5-20251001-v1:0
# BEDROCK_OCR_MODEL_ID=
# BEDROCK_EXTRACT_MODEL_ID=
# BEDROCK_RETRIEVAL_MODEL_ID=
# BEDROCK_GENERATE_MODEL_ID=
# BEDROCK_OCR_MAX_TOKENS=4096
# BEDROCK_GENERATE_MAX_TOKENS=6000
# 單張影像上限（nginx client_max_body_size 25m 已對應兩張）
# MAX_UPLOAD_BYTES=10485760
