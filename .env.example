# docker compose 用：cp .env.example .env 後改。stub 模式全部留預設即可。
ADAPTER=stub
STUB_STAGE_DELAY_S=1.5
FRONTEND_PORT=8080
BACKEND_PORT=8000
# ---- Bedrock 模式才需要（主辦方臨時憑證；不要 commit .env）----
# ADAPTER=bedrock
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# BEDROCK_REGION=us-west-2
